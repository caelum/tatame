* Commit comments only in english
* Development code only in english
* BDD and TDD with cucumber and rspec, what means that you are supposed to ALWAYS write some cucumber tests first, then the necessary rspec, then code. Always remember that this is an app to support dojo sessions
* Do NOT use scaffold ever
* If you need same script/generator, use script/generate (rspec_model/rspec_controller) instead