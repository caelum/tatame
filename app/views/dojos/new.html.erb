<%= error_messages_for :dojo %>

<% form_for :dojo, @dojo, :url => { :action => "create" } do |f| %>
	<%= label(:dojo, :date) %>
	<%= datetime_select("dojo", "date") %>
	
	<%= label(:dojo, :comment) %>
	<%= f.text_area :comment %>

	<%= submit_tag 'Create' %>
<% end %>

<%= link_to 'Back', root_path %>