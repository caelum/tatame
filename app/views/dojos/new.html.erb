<% form_for :dojo, @dojo, :url => { :action => "create" } do |f| %>
	<%= label(:dojo, :date) %>
	<%= date_select ("dojo", "date") %>
	
	<%= label(:dojo, :time) %>
	<%= time_select ("dojo", "time") %>

	<%= submit_tag 'Create' %>
<% end %>
