<h2>New Dojo</h2>

<%= error_messages_for :dojo %>

<% form_for @dojo do |f| %>
	<%= f.label :date %><br/>
	<%= f.datetime_select("date", :minute_step => 15) %><br/>
	<br/>
	<%= f.label :comment %><br/>
	<%= f.text_area :comment, :rows => 10 %><br/>
	<br/>
	<%= f.submit 'Create' %>
<% end %>

<%= link_to 'Back', root_path %>
